## fmesher based
## Tesselation/triangulation type covariates

## xx <- seq(from = 0, to = 1, length.out = 20)

## lat <- fm_lattice_2d(x = sample(xx), y = sample(xx))
## mesh <- fm_rcdt_2d(lattice = lat)
